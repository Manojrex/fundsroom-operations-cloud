import express from 'express';import cors from 'cors';import dotenv from 'dotenv';import {initDb} from './db';import {seedDemoData} from './seed';import auth from './routes/auth';import customers from './routes/customers';import products from './routes/products';import challans from './routes/challans';import dashboard from './routes/dashboard';import suppliers from './routes/suppliers';import purchases from './routes/purchases';import invoices from './routes/invoices';import reports from './routes/reports';import transfers from './routes/transfers';import audit from './routes/audit';import ai from './routes/ai';import {errorHandler} from './middleware/error';
dotenv.config();const app=express();app.use(cors({origin:process.env.CORS_ORIGIN?.split(',')||true}));app.use(express.json());app.get('/api/health',(_req,res)=>res.json({status:'ok',service:'Fundsroom ERP API',version:'4.0.0'}));app.use('/api/auth',auth);app.use('/api/customers',customers);app.use('/api/products',products);app.use('/api/challans',challans);app.use('/api/dashboard',dashboard);app.use('/api/suppliers',suppliers);app.use('/api/purchases',purchases);app.use('/api/invoices',invoices);app.use('/api/reports',reports);app.use('/api/transfers',transfers);app.use('/api/audit',audit);app.use('/api/ai',ai);app.use(errorHandler);const port=Number(process.env.PORT)||5000;initDb().then(seedDemoData).then(()=>app.listen(port,()=>console.log(`Fundsroom API running on ${port}`))).catch(e=>{console.error(e);process.exit(1)});
